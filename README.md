# Load-Shedding-Impact-Tracker
A data engineering pipeline that tracks South African load shedding stages and area schedules over time, storing them for pattern analysis — e.g. which times of day and which areas are hit hardest.
